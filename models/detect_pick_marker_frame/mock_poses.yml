# Poses the mock object-pose publisher streams, keyed by the scene IRI of the frame each
# aruco tag names. The cube's observed rest pose in the arm-base frame (the arm stands on the
# table top the world frame sits on): the scenex spawns it at z 0.04 but it settles on the
# support plane, center at its 0.02 half-size.
frame_id: base_link
objects:
  https://secorolab.github.io/models/scenes/detect-pick-marker-frame-mjc/pick_place_graph/cube/aruco_12:
    position: [0.50, 0.0, 0.02]
    orientation_xyzw: [0.0, 0.0, 0.0, 1.0]
  https://secorolab.github.io/models/scenes/detect-pick-marker-frame-mjc/pick_place_graph/cube2/aruco_13:
    position: [0.50, -0.20, 0.02]
    orientation_xyzw: [0.0, 0.0, 0.0, 1.0]
